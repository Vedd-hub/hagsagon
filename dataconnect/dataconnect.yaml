specVersion: "v1"
serviceId: "constitution-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "constitution-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
